1、键值服务器实现方式非常简单，按照描述实现不同情况下返回不同状态码即可
但是代码调试过程中遇到点小问题需要注意：go语言中，结构体作为map的value时，获取的是副本，修改副本不影响原数据
解决方案是要么用「指针类型的 value」（推荐），要么修改副本后「写回 map」。我使用的是后者
2、锁的实现方式：锁的状态保存在k/v服务器中，相同类型的锁使用相同的key，lab中以变量l传入，每个客户端生成自己的锁id，获取锁时查看锁是否被别的锁id持有，是则不断等待重试，否则获取锁
3、对于不可靠网络的处理方式，get请求只需要增加重试即可，put请求只允许重试一次，超过一次后返回maybe状态